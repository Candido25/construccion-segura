name: Diagnosticar seguridad durante la construcción

on:
  pull_request:
    branches: [main]
    paths:
      - scripts/diagnosticar_seguridad_construccion.py
      - .github/workflows/diagnosticar-seguridad-construccion.yml

permissions:
  contents: write

jobs:
  diagnosticar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ampliar-seguridad-construccion-20260727
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/diagnosticar_seguridad_construccion.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/DIAGNOSTICO_SEGURIDAD_CONSTRUCCION_2026-07-27.md
          git diff --cached --quiet || git commit -m "Registrar diagnóstico de seguridad en construcción"
          git push origin HEAD:agent/ampliar-seguridad-construccion-20260727

name: Diagnosticar obras preliminares

on:
  pull_request:
    branches:
      - main
    paths:
      - scripts/diagnosticar_obras_preliminares.py
      - .github/workflows/diagnosticar-obras-preliminares.yml

permissions:
  contents: write

jobs:
  diagnosticar:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar rama
        uses: actions/checkout@v4
        with:
          ref: agent/ampliar-obras-preliminares-seguridad-20260727

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generar diagnóstico
        run: python scripts/diagnosticar_obras_preliminares.py

      - name: Confirmar diagnóstico
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/DIAGNOSTICO_OBRAS_PRELIMINARES_2026-07-27.md
          git diff --cached --quiet || git commit -m "Registrar diagnóstico de obras preliminares"
          git push origin HEAD:agent/ampliar-obras-preliminares-seguridad-20260727
